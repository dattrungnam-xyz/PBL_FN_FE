import { Box } from "@mui/material";
import "./Footer.scss";

const Footer: React.FC = () => {
  return (
    <footer>
      <Box sx={{ width: "100%", maxWidth: "1300px", px: { md: 2, sm: 1 } }}>
        content
      </Box>
    </footer>
  );
};

export default Footer;
